# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
# dist/ is COMMITTED on purpose — see README. Installing this package from
# GitHub otherwise forces every consumer to install the full devDependency
# tree and run a build before npm can extract anything.
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

dist-docs

# design-sync (staged converter + generated output)
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
dist/design-sync-styles.css
